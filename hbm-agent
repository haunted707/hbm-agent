#!/usr/bin/env python3
"""
HBM AGENT CLI launcher.

This wrapper should behave like the installed `hbm` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from hbm_cli.main import main
    main()
